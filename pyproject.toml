[project]
name = "scripty-scripts"
version = "0.1.0"
description = "Scripts for redis auditing and utilities."
requires-python = ">=3.10"
dependencies = [
  "redis==7.1.0",
  "requests>=2.33.1",
]

[tool.uv]
package = false
