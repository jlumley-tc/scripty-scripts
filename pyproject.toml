[project]
name = "scripty-scripts"
version = "0.1.0"
description = "Scripts for redis auditing and utilities."
requires-python = ">=3.10"
dependencies = [
  "httpx==0.28.1",
  "redis==7.1.0",
]

[tool.uv]
package = false
